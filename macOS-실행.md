# macOS에서 실행하기

- Apple Silicon(M1·M2·M3·M4 등): `arm64` 파일을 사용합니다.
- Intel Mac: `x64` 파일을 사용합니다.

압축을 풀고 `Mac에서 처음 실행.command`를 한 번 실행하세요. 이 파일은:

1. 다운로드 과정에서 붙은 macOS 격리 속성을 정리합니다.
2. 주 실행 파일에 실행 권한을 설정합니다.
3. Mac에 들어 있는 `codesign`으로 앱 번들을 임시 서명합니다.
4. `TodoWidget Cross.app`을 엽니다.

스크립트 실행 자체가 차단되면 파일을 Control 클릭하고 `열기`를 선택하세요. 그래도 차단되면:

1. `TodoWidget Cross.app`을 한 번 열어 차단 메시지를 확인합니다.
2. `시스템 설정 → 개인정보 보호 및 보안`을 엽니다.
3. 보안 항목의 `그래도 열기`를 누르고 로그인 암호로 승인합니다.

앞으로 이 승인을 없애려면 개발자의 Apple Developer ID 서명과 Apple 공증이 필요합니다.
